load_module /usr/local/libexec/nginx/ngx_mail_module.so;
load_module /usr/local/libexec/nginx/ngx_stream_module.so;

#user  nobody;
worker_processes  1;

error_log  /usr/home/huser/logs/ngingx-error.log;
error_log  /usr/home/huser/logs/ngingx-error.log  notice;
error_log  /usr/home/huser/logs/ngingx-error.log  info;

#pid        logs/nginx.pid;

events {
    worker_connections  1024;
}

http {
    include       mime.types;
    default_type  application/octet-stream;

    #log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
    #                  '$status $body_bytes_sent "$http_referer" '
    #                  '"$http_user_agent" "$http_x_forwarded_for"';

    access_log  /usr/home/huser/logs/ngingx-access.log combined;

    sendfile        on;
    client_max_body_size 20M;
    #tcp_nopush     on;

    keepalive_timeout  0;
    #keepalive_timeout  65;

    #gzip  on;

    server {
        listen       80;
        server_name  localhost;
        location / {
                include uwsgi_params;
                uwsgi_pass unix:/usr/home/huser/run/uwsgi.socket;
                uwsgi_max_temp_file_size 0;
                uwsgi_read_timeout 3000;
                # uwsgi_pass 127.0.0.1:3031;
                }
        }
}